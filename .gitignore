# === OS / Editor ===
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# === Go Server ===
server/bin/
server/tmp/
server/*.exe
server/*.exe~
server/*.test
server/coverage.out
server/testdata/test_media_root/

# === Node / Frontend ===
client/node_modules/
client/dist/
client/.vite/
client/tmp/

# === Playwright MCP traces ===
.playwright-mcp/

# === Local environment ===
.env
.env.*
!.env.example
server/.env
server/.env.*
client/.env
client/.env.*

# === Local caches and logs ===
.cache/
.logs/
# Root-level build artifacts and run logs. The binary is launched from the repo
# root during local testing, so these appear here rather than under server/.
*.log
/nexora-api
/nexora-api.exe
/*.exe

# === Local generated assets ===
server/assets/**
!server/assets/images/.gitkeep

# === Local toolchains & personal dev tools ===
.tools/
.codebuddy/

# === Archive (historical reference, not needed in Git) ===
_archive/
.codebuddy
.merge-backups/

# === Reference tooling kept out of the release ===
# The resolution probe is a development aid for inspecting real libraries; it is
# not part of the server build.
